unit UnitAjuda; 

{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils, FileUtil, Forms, Controls, Graphics, Dialogs, ExtCtrls,
  StdCtrls;

type

  { TFormAjuda }

  TFormAjuda = class(TForm)
    Label1: TLabel;
    Label2: TLabel;
    Label3: TLabel;
    Label4: TLabel;
    Label5: TLabel;
    Label6: TLabel;
    Panel1: TPanel;
    procedure FormClose(Sender: TObject; var CloseAction: TCloseAction);
  private
    { private declarations }
  public
    { public declarations }
  end; 

var
  FormAjuda: TFormAjuda;

implementation

{$R *.lfm}

{ TFormAjuda }

procedure TFormAjuda.FormClose(Sender: TObject; var CloseAction: TCloseAction);
begin
  FormAjuda.Close;
end;

end.

